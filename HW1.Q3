#include <stdio.h>
#include <math.h>

int main(void)
{
	int popularity;
	int size;
	int homeValue;
	
	printf("Enter house popularity:");
	scanf("%d", &popularity);
	
	printf("\nEnter house size:");
	scanf("%d", &size);
	
	homeValue = (pow(popularity,3) + pow(size,2)) * 10000;
	printf("\nHome value is: %d\n", homeValue);
	
	return(0);
}
